{
  "name": "stylesync-cli",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "stylesync": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "sync": "npm run dev -- sync",
    "init-cfg": "npm run dev -- --init"
  }
}